# Database (Neon free tier recommended for Vercel)
# Get yours at https://neon.tech (free 0.5 GB)
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/attendance_db?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
AUTH_SECRET="generate-with-openssl-rand-base64-32"

# Email (Nodemailer) - optional
SMTP_HOST="smtp.mailgun.org"
SMTP_PORT=587
SMTP_USER="your-mailgun-smtp-login"
SMTP_PASS="your-mailgun-smtp-password"
SMTP_FROM="no-reply@yourdomain.com"

# Google Maps (optional - for map features)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=""

# App Config
NEXT_PUBLIC_APP_NAME="AttendEase"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_DEFAULT_GEOFENCE_RADIUS=200
